<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MegafonAppwidget.SimpleTextView" parent="android:Widget.TextView">
        <item name="android:textColor">@color/megafon_appwidget__textColor</item>
        <item name="android:textSize">@dimen/megafon_appwidget__textSize</item>
        <item name="android:lines">1</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>
